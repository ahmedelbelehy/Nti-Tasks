void main (){

final name = "Elbelehy";

const age = 21;

print('name is $name and age is $age');

print("final is cannot be changed in different types but const can be changed in different types.");
print("const is a compile-time component (assigned once) and ensures deep immutability for objects/collections.");

}