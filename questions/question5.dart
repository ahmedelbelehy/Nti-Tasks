void main() {
  var age = 25;
  age = 30;
  print("Age: $age");
                          //final is cannot be changed in different types but const can be changed in different types.
                          // var is cannot be changed in different types but dynamic can be changed in different types.
  
}
